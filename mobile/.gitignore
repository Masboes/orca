node_modules/
src/terminal/terminal-webview-engine.generated.ts
src/components/pr-sidebar/mermaid-webview-engine.generated.ts
.expo/
dist/
/android/
/ios/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# Klim trial fonts — evaluation only. Their licence permits internal testing but
# not redistribution, and this fork is public, so these must never be committed.
# Replace with licensed files (and remove this rule) if Tiempos is purchased.
assets/fonts/TiemposText-*.otf
